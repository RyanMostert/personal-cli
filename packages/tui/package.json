{
  "name": "@personal-cli/tui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --external ink react --external @personal-cli/core --external @personal-cli/shared --platform node",
    "start": "node dist/cli.js",
    "test": "vitest"
  }
}
